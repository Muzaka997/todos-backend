services:
  - type: web
    name: todos-backend
    env: node
    region: oregon
    plan: free
    buildCommand: npm install && npm run build && npm run db:migrate
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
